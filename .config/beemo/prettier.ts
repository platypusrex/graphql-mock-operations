import { PrettierConfig } from '@beemo/driver-prettier';

const config: PrettierConfig = {
  useTabs: false,
  printWidth: 100,
  singleQuote: true,
  trailingComma: 'es5',
  semi: true,
};

export default config;
